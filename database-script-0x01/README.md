# Schema Definition

This folder contains the SQL schema (`schema.sql`) used to define the relational database structure for the Airbnb-like app.

It includes:
- Users
- Properties
- Bookings
- Payments

Each table is connected with proper primary and foreign key constraints, and indexing is applied where relevant.
